server:
  port: 8021
spring:
  profiles:
    active: dev
    #include: redis，jdbc
    include: redis-cluster,jdbc,mybatis
logging:
  level:
    com:
      lll:
        sort:
          dao:
            mapper: debug